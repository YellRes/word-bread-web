# 计划 A：练习页增强与数据基建 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给练习页加键盘流与"提示+重试"拼写提示，并落地 `BlankAttempt` 词级数据采集，同时建立 vitest 纯函数测试基建。

**Architecture:** 纯前端改动集中在 `PracticePage.tsx`；新增一个写库函数 `recordBlankAttempts` 写入新表 `BlankAttempt`；UI 行为用 preview 工具人工验证，纯函数（sm2/annotation）用 vitest 单测。落库严格"首次判定、只记一次"。

**Tech Stack:** React 19 + TypeScript + Vite + Supabase + vitest。

---

## 文件结构

新增：
- `vitest.config.ts` 由 `vite.config.ts` 合并 test 配置（见 Task 1）
- `src/lib/sm2.test.ts` — SM-2 纯函数测试
- `src/lib/annotation.test.ts` — 标注解析纯函数测试

修改：
- `vite.config.ts` — 加 vitest test 配置
- `package.json` — 加 test 脚本
- `src/services/practiceService.ts` — 加 `recordBlankAttempts`
- `src/pages/PracticePage.tsx` — #6 键盘流、#5 拼写提示+重试、自动跳题竞态修复、首次落库接 `recordBlankAttempts`

Supabase（人工）：建表 `BlankAttempt`（Task 5）。

---

## Task 1: 引入 vitest 测试基建

**Files:**
- Modify: `vite.config.ts`
- Modify: `package.json`

- [ ] **Step 1: 安装 vitest**

Run: `npm i -D vitest`
Expected: 安装成功，`package.json` devDependencies 出现 `vitest`。

- [ ] **Step 2: 给 vite.config.ts 加 test 配置**

把 `vite.config.ts` 顶部的 `import { defineConfig } from 'vite'` 改为从 vitest 引入，并加 `test` 字段。最终文件：

```ts
/// <reference types="vitest/config" />
import path from 'node:path'
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: { '@': path.resolve(__dirname, './src') },
  },
  server: {
    // 尊重外部注入的 PORT（如 preview 工具分配的端口）
    port: process.env.PORT ? Number(process.env.PORT) : undefined,
  },
  test: {
    environment: 'node',
    globals: true,
    include: ['src/**/*.test.ts'],
  },
})
```

> 注意：保留你现有 `vite.config.ts` 里的 plugins / alias / server 字段原样，只新增 `test` 与改 import。若现有字段与此处不同，以现有为准、只追加 `test`。

- [ ] **Step 3: 加 package.json 测试脚本**

在 `package.json` scripts 中加入：

```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 4: 验证 vitest 可运行**

Run: `npm test`
Expected: 提示 "No test files found" 或 0 通过（此时还没测试文件），命令本身不报错退出。

- [ ] **Step 5: Commit**

```bash
git add vite.config.ts package.json package-lock.json
git commit -m "chore: 引入 vitest 测试基建"
```

---

## Task 2: SM-2 纯函数单测

**Files:**
- Test: `src/lib/sm2.test.ts`

- [ ] **Step 1: 写测试**

```ts
import { describe, it, expect } from 'vitest'
import { gradeFrom, sm2, nextReviewIso, INITIAL_SM2 } from './sm2'

describe('gradeFrom', () => {
  it('显示答案记为 1', () => {
    expect(gradeFrom({ revealed: true, correctBlanks: 2, totalBlanks: 2 })).toBe(1)
  })
  it('全对记为 5', () => {
    expect(gradeFrom({ revealed: false, correctBlanks: 2, totalBlanks: 2 })).toBe(5)
  })
  it('对一半以上(>=0.6)记为 3', () => {
    expect(gradeFrom({ revealed: false, correctBlanks: 3, totalBlanks: 5 })).toBe(3)
  })
  it('对得少(<0.6)记为 2', () => {
    expect(gradeFrom({ revealed: false, correctBlanks: 1, totalBlanks: 5 })).toBe(2)
  })
})

describe('sm2', () => {
  it('首次答对：reps0 -> 间隔 1 天', () => {
    const s = sm2(INITIAL_SM2, 5)
    expect(s.reps).toBe(1)
    expect(s.intervalDays).toBe(1)
  })
  it('第二次答对：reps1 -> 间隔 6 天', () => {
    const s = sm2({ reps: 1, intervalDays: 1, ease: 2.5, lapses: 0 }, 5)
    expect(s.intervalDays).toBe(6)
  })
  it('第三次答对：间隔 = round(interval*ease)', () => {
    const s = sm2({ reps: 2, intervalDays: 6, ease: 2.5, lapses: 0 }, 5)
    expect(s.intervalDays).toBe(Math.round(6 * s.ease))
  })
  it('答错：reps 归零、间隔回到 1、lapses+1', () => {
    const s = sm2({ reps: 3, intervalDays: 30, ease: 2.5, lapses: 0 }, 2)
    expect(s.reps).toBe(0)
    expect(s.intervalDays).toBe(1)
    expect(s.lapses).toBe(1)
  })
  it('ease 下限 1.3', () => {
    let s: typeof INITIAL_SM2 = { reps: 0, intervalDays: 0, ease: 1.3, lapses: 0 }
    s = sm2(s, 0)
    expect(s.ease).toBeGreaterThanOrEqual(1.3)
  })
})

describe('nextReviewIso', () => {
  it('在 base 上加 intervalDays 天', () => {
    const base = new Date('2026-01-01T00:00:00.000Z')
    const iso = nextReviewIso(6, base)
    expect(new Date(iso).getTime()).toBe(base.getTime() + 6 * 86400000)
  })
})
```

- [ ] **Step 2: 跑测试确认通过**

Run: `npm test -- src/lib/sm2.test.ts`
Expected: PASS（全部用例通过）。若某用例失败，说明对实现的理解有误——读 `src/lib/sm2.ts` 修正用例预期，而非改实现。

- [ ] **Step 3: Commit**

```bash
git add src/lib/sm2.test.ts
git commit -m "test: SM-2 纯函数单测"
```

---

## Task 3: 标注解析纯函数单测

**Files:**
- Test: `src/lib/annotation.test.ts`

- [ ] **Step 1: 写测试**

```ts
import { describe, it, expect } from 'vitest'
import { splitAnnotation, parseContent, hasBlanks, normalizeAnswer, toPlainSentence } from './annotation'

describe('splitAnnotation', () => {
  it('按首个中文字符切分英文/中文', () => {
    expect(splitAnnotation('pen-笔')).toEqual({ answer: 'pen', hint: '笔' })
  })
  it('多词答案保留空格', () => {
    expect(splitAnnotation('trade surplus-贸易顺差')).toEqual({ answer: 'trade surplus', hint: '贸易顺差' })
  })
  it('英文连字符不被误切（无中文时回退到最后一个 -）', () => {
    expect(splitAnnotation('ice-cream')).toEqual({ answer: 'ice', hint: 'cream' })
  })
  it('无分隔时整串作答案、无释义', () => {
    expect(splitAnnotation('hello')).toEqual({ answer: 'hello', hint: '' })
  })
})

describe('parseContent', () => {
  it('拆成 文本段 / 空格段', () => {
    const segs = parseContent('I have a (pen-笔)')
    expect(segs).toEqual([
      { type: 'text', value: 'I have a ' },
      { type: 'blank', answer: 'pen', hint: '笔' },
    ])
  })
})

describe('hasBlanks', () => {
  it('含括号标注返回 true', () => {
    expect(hasBlanks('a (pen-笔)')).toBe(true)
  })
  it('无标注返回 false', () => {
    expect(hasBlanks('a pen')).toBe(false)
  })
})

describe('normalizeAnswer', () => {
  it('去首尾空格 + 小写 + 压内部空格', () => {
    expect(normalizeAnswer('  Trade   Surplus ')).toBe('trade surplus')
  })
})

describe('toPlainSentence', () => {
  it('拼出纯英文句子（去中文与括号）', () => {
    expect(toPlainSentence('I have a (pen-笔).')).toBe('I have a pen.')
  })
})
```

- [ ] **Step 2: 跑测试确认通过**

Run: `npm test -- src/lib/annotation.test.ts`
Expected: PASS。若失败，读 `src/lib/annotation.ts` 核对真实行为后修正用例预期。

- [ ] **Step 3: Commit**

```bash
git add src/lib/annotation.test.ts
git commit -m "test: 标注解析纯函数单测"
```

---

## Task 4: #6 键盘流 + 自动跳题竞态修复

**Files:**
- Modify: `src/pages/PracticePage.tsx`

- [ ] **Step 1: 加入 input ref 与 timer ref**

在 `PracticePage` 组件内、`useState` 声明区附近加入两个 ref（需确保文件顶部已 `import { useEffect, useMemo, useRef, useState } from 'react'`，当前是 `useEffect, useMemo, useState`，补上 `useRef`）：

```ts
const inputRefs = useRef<(HTMLInputElement | null)[]>([])
const advanceTimer = useRef<number | null>(null)
```

- [ ] **Step 2: 切句 effect 中重置并自动聚焦第一个空、清理 timer**

把现有"切句时重置作答态"的 effect 改为（在重置后聚焦第一个空，并在依赖变化/卸载时清理待跳转的 timer）：

```ts
// 切句时重置作答态 + 自动聚焦第一个空
useEffect(() => {
  setInputs(Array(blanks.length).fill(''))
  setChecked(false)
  setRevealed(false)
  setBlankResults([])
  setWordShown(Array(blanks.length).fill(false))
  setStage('first')
  setFirstResults([])
  // 清理上一句可能挂着的自动跳题计时器
  if (advanceTimer.current !== null) {
    clearTimeout(advanceTimer.current)
    advanceTimer.current = null
  }
  // 渲染后聚焦第一个空
  const id = requestAnimationFrame(() => inputRefs.current[0]?.focus())
  return () => cancelAnimationFrame(id)
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, [index, current?.id])
```

> `setStage` / `setFirstResults` 在 Task 6 定义；本步先写入，Task 6 补齐 state 声明后即可编译。若希望分步可编译，可先只加 ref + 聚焦 + timer 清理，stage/firstResults 两行随 Task 6 一起加。

- [ ] **Step 3: 组件卸载时清理 timer**

新增一个仅卸载时运行的 effect：

```ts
useEffect(() => () => {
  if (advanceTimer.current !== null) clearTimeout(advanceTimer.current)
}, [])
```

- [ ] **Step 4: 给每个填空 input 绑定 ref 与 Tab 跳转**

在渲染填空 `<input>` 的地方（`segments.map` 内 `type === 'blank'` 分支），给 input 增加 `ref` 与 `onKeyDown` 的 Tab 处理（保留原有 Enter 检查逻辑，Enter 行为在 Task 6 调整）。input props 增加：

```tsx
ref={el => { inputRefs.current[k] = el }}
onKeyDown={e => {
  if (e.key === 'Enter') { e.preventDefault(); handlePrimaryAction() }
  else if (e.key === 'Tab' && !e.shiftKey) {
    const nextEditable = inputRefs.current.findIndex((el, i) => i > k && el && !el.disabled)
    if (nextEditable !== -1) { e.preventDefault(); inputRefs.current[nextEditable]?.focus() }
  }
}}
```

> `handlePrimaryAction` 在 Task 6 定义（根据 stage 决定是"检查"还是"下一句"）。Shift+Tab 用浏览器原生行为，无需特殊处理。

- [ ] **Step 5: 人工验证（preview 工具）**

启动 dev：preview_start（name: dev）。进入任一文章练习，确认：
1. 进入新句后焦点自动落在第一个空（用 preview_eval 检查 `document.activeElement.tagName === 'INPUT'`）。
2. Tab 能在空格间移动。
3. 这一步因依赖 Task 6 的 `handlePrimaryAction`/`stage` 才能编译运行——**Task 4 与 Task 6 作为一组连续完成后再统一验证**。

- [ ] **Step 6: Commit（与 Task 6 合并提交，见 Task 6 Step 末）**

本任务不单独提交，与 Task 6 一起提交（二者代码相互依赖）。

---

## Task 5: 建表 `BlankAttempt`（Supabase，人工）

**Files:**
- 无代码文件；在 Supabase SQL Editor 执行。

- [ ] **Step 1: 在 Supabase 执行建表 SQL**

```sql
create table "BlankAttempt" (
  id text primary key,
  "sentenceId" text,
  "articleId" text,
  word text,
  hint text,
  "isCorrect" boolean,
  revealed boolean,
  "createdAt" timestamptz default now()
);
create index "BlankAttempt_word_idx" on "BlankAttempt" (word);
create index "BlankAttempt_createdAt_idx" on "BlankAttempt" ("createdAt");
```

- [ ] **Step 2: 设置 RLS 与现有表一致**

参照现有 `PracticeRecord` 表的 RLS 策略，对 `BlankAttempt` 应用相同的 insert/select 策略（本项目无鉴权、依赖 anon key，需保证 anon 角色可 insert/select）。

- [ ] **Step 3: 验证可写入**

在 SQL Editor 执行一次测试插入与查询，确认成功后删除测试行：

```sql
insert into "BlankAttempt" (id, "sentenceId", "articleId", word, hint, "isCorrect", revealed)
values ('test-1','s','a','probe','探究',false,false);
select * from "BlankAttempt" where id='test-1';
delete from "BlankAttempt" where id='test-1';
```

Expected: 插入成功、能查到、删除成功。

---

## Task 6: #5 拼写提示（提示+重试，按首次判定）+ 首次落库接 BlankAttempt

**Files:**
- Modify: `src/services/practiceService.ts`
- Modify: `src/pages/PracticePage.tsx`

- [ ] **Step 1: 在 practiceService.ts 加 `recordBlankAttempts`**

文件顶部加 `import { normalizeAnswer } from '../lib/annotation'`，并新增：

```ts
export interface BlankAttemptInput {
  sentenceId: string
  articleId: string
  blanks: { word: string; hint: string }[]
  firstResults: boolean[]
  revealed: boolean
}

/** 按"首次作答"逐空写入 BlankAttempt（词级易错统计来源）。失败仅记录日志，不阻断练习。 */
export const recordBlankAttempts = async (input: BlankAttemptInput): Promise<boolean> => {
  if (input.blanks.length === 0) return true
  const rows = input.blanks.map((b, i) => ({
    id: crypto.randomUUID(),
    sentenceId: input.sentenceId,
    articleId: input.articleId,
    word: normalizeAnswer(b.word),
    hint: b.hint,
    isCorrect: input.firstResults[i] ?? false,
    revealed: input.revealed,
  }))
  const { error } = await supabase.from('BlankAttempt').insert(rows)
  if (error) {
    console.error('Error recording blank attempts:', error)
    return false
  }
  return true
}
```

- [ ] **Step 2: PracticePage 引入新 state 与导入**

顶部 import 增加 `recordBlankAttempts`：

```ts
import { recordPractice, recordBlankAttempts } from '../services/practiceService'
```

在 state 区新增（替代原先仅靠 `checked` 的单段判定）：

```ts
type Stage = 'first' | 'retry' | 'done'
const [stage, setStage] = useState<Stage>('first')
const [firstResults, setFirstResults] = useState<boolean[]>([])
```

`checked` 仍保留用于"是否已揭示/锁定显示"，等价于 `stage !== 'first' && stage !== 'retry'` 中的 done；为减少改动，约定：`checked = stage === 'done'`。删除独立的 `setChecked` 调用，改为通过 `setStage` 推导（见下）。在用到 `checked` 的渲染处，用 `const checked = stage === 'done'` 局部派生。

> 具体做法：删除 `const [checked, setChecked] = useState(false)`，在组件内（segments/blanks 之后）加 `const checked = stage === 'done'`；切句 effect 里删掉 `setChecked(false)`（已由 `setStage('first')` 覆盖）。

- [ ] **Step 3: 用首字母+长度占位生成提示串**

在组件内加工具（紧邻 `blankIdx` 计算前）：

```ts
const hintPlaceholder = (answer: string) =>
  answer.length <= 1 ? answer : answer[0] + '·'.repeat(answer.length - 1)
```

- [ ] **Step 4: 重写检查/重试/揭示/落库逻辑**

用以下实现替换现有 `handleCheck` / `handleReveal`，并新增 `handlePrimaryAction`、`persistFirst`：

```ts
// 首次落库（且只此一次）：PracticeRecord + SM-2 + BlankAttempt，全部用首次结果
const persistFirst = (results: boolean[], wasRevealed: boolean) => {
  if (!current) return
  const correctBlanks = results.filter(Boolean).length
  const allCorrect = correctBlanks === blanks.length && !wasRevealed
  recordPractice({
    sentenceId: current.id,
    articleId: current.articleId,
    isCorrect: allCorrect,
    totalBlanks: blanks.length,
    correctBlanks,
    revealed: wasRevealed,
  }).then(ok => { if (!ok) toast.error('进度保存失败，但不影响继续练习') })
  upsertReview({
    sentenceId: current.id,
    articleId: current.articleId,
    revealed: wasRevealed,
    correctBlanks,
    totalBlanks: blanks.length,
  }).catch(e => console.error('upsertReview failed:', e))
  recordBlankAttempts({
    sentenceId: current.id,
    articleId: current.articleId,
    blanks: blanks.map(b => ({ word: b.answer, hint: b.hint })),
    firstResults: results,
    revealed: wasRevealed,
  }).catch(e => console.error('recordBlankAttempts failed:', e))
}

const handleCheck = () => {
  const results = blanks.map((b, k) => normalizeAnswer(inputs[k] ?? '') === normalizeAnswer(b.answer))
  setFirstResults(results)
  setBlankResults(results)
  setWordShown(blanks.map(() => true))
  persistFirst(results, false) // 只在首次检查落库
  const allCorrect = results.every(Boolean)
  if (allCorrect) {
    setStage('done')
    setCorrectCount(c => c + 1)
    const isLast = index >= sentences.length - 1
    toast.success(isLast ? '全部答对！查看结果' : '全部答对！进入下一题')
    advanceTimer.current = window.setTimeout(() => handleNext(), 900)
  } else {
    // 进入重试态：清空答错的空，露出首字母提示；答对的空保持只读+绿
    setStage('retry')
    setInputs(prev => prev.map((v, k) => (results[k] ? v : '')))
    toast.error(`还有 ${results.filter(r => !r).length} 个填错了，给了首字母再试一次`)
    requestAnimationFrame(() => {
      const firstWrong = results.findIndex(r => !r)
      if (firstWrong !== -1) inputRefs.current[firstWrong]?.focus()
    })
  }
}

const handleRetryCheck = () => {
  // 重试不再落库；揭示完整答案并标记
  const results = blanks.map((b, k) => normalizeAnswer(inputs[k] ?? '') === normalizeAnswer(b.answer))
  setBlankResults(results)
  setWordShown(blanks.map(() => true))
  setStage('done')
}

const handleReveal = () => {
  const results = blanks.map((b, k) => normalizeAnswer(inputs[k] ?? '') === normalizeAnswer(b.answer))
  setBlankResults(results)
  setWordShown(blanks.map(() => true))
  setInputs(blanks.map(b => b.answer))
  setRevealed(true)
  if (stage === 'first') {
    setFirstResults(results)
    persistFirst(results, true) // 第一阶段就放弃 → 按 revealed 落库
  }
  setStage('done')
}

// Enter 主操作：按当前阶段决定"检查 / 重试检查 / 下一句"
const handlePrimaryAction = () => {
  if (stage === 'first') handleCheck()
  else if (stage === 'retry') handleRetryCheck()
  else handleNext()
}
```

同时把 `handleNext` 改为进入前清理待跳转 timer：

```ts
const handleNext = () => {
  if (advanceTimer.current !== null) { clearTimeout(advanceTimer.current); advanceTimer.current = null }
  if (index < sentences.length - 1) setIndex(i => i + 1)
  else setPhase('summary')
}
```

> 删除原 `persist` 函数（其职责已并入 `persistFirst`）。确认无其他调用点。

- [ ] **Step 5: 调整 input 的禁用与占位（重试态下答错的空可编辑且显示首字母提示）**

在填空 `<input>` 渲染处：
- `disabled` 改为：`stage === 'done' || (stage === 'retry' && firstResults[k])`（done 全锁；retry 时只锁答对的）。
- 新增 `placeholder`：`stage === 'retry' && !firstResults[k] ? hintPlaceholder(seg.answer) : undefined`。
- 颜色类沿用现有 `ok`/`wrong`（基于 `checked && blankResults[k]`），其中 `checked = stage === 'done'`；retry 态下答对的空也应显示绿色，故把判定改为：

```tsx
const ok = (stage === 'done' || (stage === 'retry' && firstResults[k])) && blankResults[k]
const wrong = stage === 'done' && !blankResults[k]
```

- [ ] **Step 6: 调整底部按钮区（按 stage 切换）**

把 CardFooter 内的按钮逻辑改为三态：

```tsx
<div className="flex gap-2">
  {stage === 'first' && (
    <>
      <Button variant="ghost" onClick={handleReveal}>显示答案</Button>
      <Button onClick={handleCheck}>检查</Button>
    </>
  )}
  {stage === 'retry' && (
    <>
      <Button variant="ghost" onClick={handleReveal}>显示答案</Button>
      <Button onClick={handleRetryCheck}>再检查</Button>
    </>
  )}
  {stage === 'done' && (
    <Button onClick={handleNext}>
      {index < sentences.length - 1 ? '下一句' : '查看结果'}
    </Button>
  )}
</div>
```

左侧反馈文案：把 `allCorrect` 判定改为基于首次结果——

```tsx
const allCorrect = stage === 'done' && !revealed && firstResults.length > 0 && firstResults.every(Boolean)
```

（其余 `答对了 ✓` / `继续加油` 文案保留。）

- [ ] **Step 7: 类型检查**

Run: `npx tsc -b --noEmit`
Expected: No errors found。若报错，按提示补齐 `checked` 派生、删除遗留 `persist`/`setChecked` 引用。

- [ ] **Step 8: 人工验证（preview 工具）**

preview_start(dev) → 进入练习，用 preview_eval 驱动验证：
1. **全对路径**：填对所有空 → 检查 → 出现"全部答对"toast，约 900ms 后题号 +1。
2. **错误+重试路径**：故意填错 → 检查 → 答错的空被清空且 placeholder 显示首字母（如 `p····`），答对的空标绿锁定，出现"给了首字母再试一次"toast；改对 → 再检查 → 揭示标记 → 出现"下一句"。
3. **落库只一次**：retry 后再检查不应再次写库（代码上 `handleRetryCheck` 不调用 `persistFirst`）。
4. **竞态**：全对后 900ms 内手动点别处/切句不应跳两题（已 clearTimeout）。
5. 键盘流：进入新句自动聚焦第一个空、Tab 跳转、Enter 触发主操作。

- [ ] **Step 9: Commit（含 Task 4）**

```bash
git add src/pages/PracticePage.tsx src/services/practiceService.ts
git commit -m "feat: 练习页键盘流 + 拼写提示(提示+重试，首次判定) + 词级落库"
```

---

## Task 7: 运行全部测试与类型检查收尾

**Files:** 无

- [ ] **Step 1: 跑全部单测**

Run: `npm test`
Expected: sm2 与 annotation 全部 PASS。

- [ ] **Step 2: 类型检查 + lint**

Run: `npx tsc -b --noEmit && npm run lint`
Expected: tsc 无错误；lint 不新增错误（本计划未引入新的 `any`）。

- [ ] **Step 3: 最终人工回归**

按 Task 6 Step 8 的 5 条逐项确认后，本计划完成。

---

## 自检记录

- **Spec 覆盖**：#6 键盘流（Task 4）、#5 拼写提示+重试+首次判定（Task 6）、自动跳题竞态修复（Task 4/6）、`BlankAttempt` 建表与落库（Task 5/6）、纯函数测试基建（Task 1-3）。仪表盘(#2)与扩展闭环(#3)不在本计划，属计划 B/C。
- **占位符**：无 TBD/泛化步骤；UI 步骤给出具体 props 与判定表达式。
- **类型一致性**：`recordBlankAttempts` 的 `BlankAttemptInput` 字段与 Task 6 调用一致；`Stage`/`stage`/`firstResults`/`advanceTimer`/`inputRefs`/`handlePrimaryAction` 在引用前均已定义。
- **已知顺序约束**：Task 4 与 Task 6 相互依赖，需连续完成、合并提交（已在步骤中注明）；Task 5 建表须在 Task 6 落库验证前完成。
