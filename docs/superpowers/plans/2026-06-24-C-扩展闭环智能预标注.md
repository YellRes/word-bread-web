# 计划 C：扩展闭环（智能预标注） 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在任意网页选中英文句子 → 浮出「存入 WordBread」→ 弹窗自动用已学词库预标注（`(word-中文)`）、可微调、选目标文章 → 写回 Supabase，存进来即可在网页端练习。

**Architecture:** content script 负责"选区按钮 + 弹窗 UI（shadow DOM 隔离）"，写库经 background service worker（content script 的 fetch 受页面 CORS 限制，background 有 host 权限）。预标注复用扩展已维护的 `wordMap` 与标注格式。v1 用"可编辑的预标注文本框"做微调（自动包裹已学词，用户增删括号），chip 式点选编辑列为后续增强。

**Tech Stack:** 原生 MV3 扩展（无构建），Supabase REST。

**前置依赖：** 无强依赖；但"存进来即可练"依赖网页端既有的挖空逻辑（已存在）。

---

## 文件结构

修改：
- `extension/lib/annotation.js` — 新增 `buildAnnotatedContent(text, wordMap)`（按词库自动包裹标注）
- `extension/background.js` — 新增消息处理：拉文章列表、确保"网页收藏"文章、写入句子
- `extension/content.js` — 新增：选区浮动按钮 + 存入弹窗（shadow DOM）

无需改 `manifest.json`（`host_permissions` 已含 Supabase 域、content script 已匹配 `<all_urls>`、已注入 `config.js`/`lib/annotation.js`/`content.js`）。

---

## Task 1: annotation.js 新增 `buildAnnotatedContent`

**Files:**
- Modify: `extension/lib/annotation.js`

- [ ] **Step 1: 追加函数与导出**

在 `extension/lib/annotation.js` 末尾的导出块**之前**插入：

```js
function wbEscapeRegExp(s) {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')
}

/** 用 wordMap 的键构建匹配正则：长短语优先、空白放宽、词边界。 */
function wbBuildWordRegex(wordMap) {
  var keys = Object.keys(wordMap || {})
  if (keys.length === 0) return null
  keys.sort(function (a, b) { return b.length - a.length })
  var alts = keys.map(function (k) { return wbEscapeRegExp(k).replace(/\s+/g, '\\s+') })
  return new RegExp('\\b(?:' + alts.join('|') + ')\\b', 'gi')
}

/**
 * 把纯文本里"词库已有的词"自动包裹成 (原词-中文)。未命中的文本原样保留。
 * wordMap[norm] = { display, hint }，norm 为小写压空格的键（与 background 一致）。
 */
function buildAnnotatedContent(text, wordMap) {
  var re = wbBuildWordRegex(wordMap)
  if (!re) return text
  return text.replace(re, function (m) {
    var norm = m.toLowerCase().replace(/\s+/g, ' ')
    var entry = wordMap[norm]
    var hint = entry && entry.hint ? entry.hint : ''
    return '(' + m + (hint ? ('-' + hint) : '') + ')'
  })
}
```

并把文件底部的导出块改为同时暴露新函数：

```js
// 暴露给 service worker / content script 全局
if (typeof self !== 'undefined') {
  self.extractAnnotations = extractAnnotations
  self.wbSplitAnnotation = wbSplitAnnotation
  self.buildAnnotatedContent = buildAnnotatedContent
}
```

- [ ] **Step 2: 快速自测（Node 一次性验证逻辑，不入测试套件）**

Run（在 `extension/` 目录）:
```bash
node -e "global.self={}; require('./lib/annotation.js'); console.log(self.buildAnnotatedContent('The probe found culprits here', {'probe':{display:'probe',hint:'探究'},'culprits':{display:'culprits',hint:'肇事者'}}))"
```
Expected 输出包含：`The (probe-探究) found (culprits-肇事者) here`

- [ ] **Step 3: Commit**

```bash
git add extension/lib/annotation.js
git commit -m "feat(ext): 词库自动预标注 buildAnnotatedContent"
```

---

## Task 2: background.js 写库消息处理

**Files:**
- Modify: `extension/background.js`

- [ ] **Step 1: 加 REST 辅助与处理函数**

在 `background.js` 内（`refreshWordMap` 之后、`onMessage` 监听之前）加入：

```js
function wbHeaders(extra) {
  return Object.assign({
    apikey: WB_CONFIG.key,
    Authorization: `Bearer ${WB_CONFIG.key}`,
  }, extra || {})
}

/** 拉文章列表 [{id,title}]，按创建时间倒序。 */
async function fetchArticlesRest() {
  const res = await fetch(`${WB_CONFIG.url}/rest/v1/Article?select=id,title&order=createdAt.desc`, {
    headers: wbHeaders(),
  })
  if (!res.ok) throw new Error('HTTP ' + res.status)
  return res.json()
}

/** 确保"网页收藏"文章存在，返回其 id。 */
async function ensureInboxArticle() {
  const title = '网页收藏'
  const q = await fetch(
    `${WB_CONFIG.url}/rest/v1/Article?select=id&title=eq.${encodeURIComponent(title)}&limit=1`,
    { headers: wbHeaders() }
  )
  if (q.ok) {
    const rows = await q.json()
    if (rows && rows[0]) return rows[0].id
  }
  const id = crypto.randomUUID()
  const ins = await fetch(`${WB_CONFIG.url}/rest/v1/Article`, {
    method: 'POST',
    headers: wbHeaders({ 'Content-Type': 'application/json' }),
    body: JSON.stringify({ id, title }),
  })
  if (!ins.ok) throw new Error('create inbox HTTP ' + ins.status)
  return id
}

/** 写入一条句子。articleId 为空时存入"网页收藏"。 */
async function saveSentenceRest({ content, translate, articleId }) {
  const finalArticleId = articleId || (await ensureInboxArticle())
  const res = await fetch(`${WB_CONFIG.url}/rest/v1/Sentence`, {
    method: 'POST',
    headers: wbHeaders({ 'Content-Type': 'application/json' }),
    body: JSON.stringify({
      id: crypto.randomUUID(),
      content,
      articleId: finalArticleId,
      translate: translate || '',
    }),
  })
  if (!res.ok) throw new Error('save HTTP ' + res.status)
  return true
}
```

- [ ] **Step 2: 在 onMessage 中接入两个新消息**

把现有 `chrome.runtime.onMessage.addListener` 改为同时处理 `wb-articles` 与 `wb-save`（保留原 `wb-refresh` 分支）：

```js
chrome.runtime.onMessage.addListener((msg, _sender, sendResponse) => {
  if (msg && msg.type === 'wb-refresh') {
    refreshWordMap().then(count => sendResponse({ count }))
    return true
  }
  if (msg && msg.type === 'wb-articles') {
    fetchArticlesRest()
      .then(articles => sendResponse({ ok: true, articles }))
      .catch(e => { console.error('[WordBread] 拉文章失败', e); sendResponse({ ok: false, error: String(e) }) })
    return true
  }
  if (msg && msg.type === 'wb-save') {
    saveSentenceRest(msg.payload || {})
      .then(() => { refreshWordMap(); sendResponse({ ok: true }) }) // 存完顺手刷新词表
      .catch(e => { console.error('[WordBread] 存句失败', e); sendResponse({ ok: false, error: String(e) }) })
    return true
  }
})
```

- [ ] **Step 3: Commit**

```bash
git add extension/background.js
git commit -m "feat(ext): background 写库消息（文章列表/确保收藏夹/存句）"
```

---

## Task 3: content.js 选区按钮 + 存入弹窗

**Files:**
- Modify: `extension/content.js`

- [ ] **Step 1: 在 IIFE 内追加捕获模块**

在 `content.js` 的 IIFE 内、`init()` 定义之前，插入以下自包含模块（复用闭包变量 `wordMap` 与全局 `buildAnnotatedContent`）：

```js
  // ===== 捕获：选中网页句子 → 存入 WordBread =====
  var captureBtn = null
  var captureHost = null // shadow DOM 宿主

  function removeCaptureBtn() {
    if (captureBtn) { captureBtn.remove(); captureBtn = null }
  }

  function onSelectionChange() {
    var sel = window.getSelection()
    var text = sel && sel.toString().trim()
    // 仅对"像一句话"的英文选区显示：有内容、长度适中、含字母
    if (!text || text.length < 6 || text.length > 400 || !/[a-zA-Z]/.test(text)) {
      removeCaptureBtn(); return
    }
    if (captureHost) return // 弹窗开着时不重复弹按钮
    showCaptureBtn(sel)
  }

  function showCaptureBtn(sel) {
    removeCaptureBtn()
    var range = sel.getRangeAt(0)
    var rect = range.getBoundingClientRect()
    captureBtn = document.createElement('button')
    captureBtn.textContent = '＋ 存入 WordBread'
    captureBtn.className = 'wb-capture-btn'
    captureBtn.style.cssText =
      'position:absolute;z-index:2147483646;font:600 12px/1 system-ui,sans-serif;' +
      'background:#d97706;color:#fff;border:none;border-radius:6px;padding:6px 10px;' +
      'cursor:pointer;box-shadow:0 2px 8px rgba(0,0,0,.25);'
    captureBtn.style.top = (rect.bottom + window.scrollY + 6) + 'px'
    captureBtn.style.left = (rect.left + window.scrollX) + 'px'
    var captured = sel.toString().trim()
    captureBtn.addEventListener('mousedown', function (e) { e.preventDefault() }) // 防止点击时丢失选区
    captureBtn.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation()
      removeCaptureBtn()
      openCapturePanel(captured)
    })
    document.body.appendChild(captureBtn)
  }

  function closeCapturePanel() {
    if (captureHost) { captureHost.remove(); captureHost = null }
  }

  function openCapturePanel(text) {
    closeCapturePanel()
    captureHost = document.createElement('div')
    captureHost.style.cssText = 'position:fixed;z-index:2147483647;inset:0;'
    var shadow = captureHost.attachShadow({ mode: 'open' })
    var annotated = (typeof buildAnnotatedContent === 'function')
      ? buildAnnotatedContent(text, wordMap) : text
    shadow.innerHTML =
      '<style>' +
      ':host{all:initial}' +
      '.mask{position:fixed;inset:0;background:rgba(0,0,0,.35);display:flex;align-items:center;justify-content:center;font-family:system-ui,sans-serif}' +
      '.card{background:#fff;color:#1a1a1a;width:min(560px,92vw);border-radius:14px;padding:18px;box-shadow:0 12px 40px rgba(0,0,0,.3)}' +
      '.card h3{margin:0 0 10px;font-size:15px}' +
      '.lbl{font-size:12px;color:#666;margin:10px 0 4px}' +
      'textarea,select{width:100%;box-sizing:border-box;border:1px solid #ddd;border-radius:8px;padding:8px;font:13px/1.6 system-ui;color:#1a1a1a;background:#fff}' +
      'textarea{resize:vertical}' +
      '.hint{font-size:11px;color:#999;margin-top:4px}' +
      '.row{display:flex;gap:8px;justify-content:flex-end;margin-top:14px}' +
      'button{font:600 13px system-ui;border-radius:8px;padding:8px 14px;cursor:pointer;border:1px solid #ddd;background:#fff}' +
      '.save{background:#d97706;color:#fff;border-color:#d97706}' +
      '.err{color:#dc2626;font-size:12px;margin-top:8px;min-height:14px}' +
      '</style>' +
      '<div class="mask">' +
      '<div class="card">' +
      '<h3>存入 WordBread</h3>' +
      '<div class="lbl">句子（已自动标注你学过的词，可改：用 (word-中文) 标记要挖空的生词）</div>' +
      '<textarea id="wb-content" rows="3"></textarea>' +
      '<div class="hint">至少标注一个 (词-中文) 才能用于练习。</div>' +
      '<div class="lbl">中文翻译（可选）</div>' +
      '<textarea id="wb-translate" rows="2"></textarea>' +
      '<div class="lbl">存到文章</div>' +
      '<select id="wb-article"><option value="">网页收藏（默认）</option></select>' +
      '<div class="err" id="wb-err"></div>' +
      '<div class="row">' +
      '<button id="wb-cancel">取消</button>' +
      '<button class="save" id="wb-save">存入</button>' +
      '</div></div></div>'
    document.body.appendChild(captureHost)

    var $ = function (id) { return shadow.getElementById(id) }
    $('wb-content').value = annotated
    $('wb-cancel').addEventListener('click', closeCapturePanel)
    shadow.querySelector('.mask').addEventListener('click', function (e) {
      if (e.target.classList.contains('mask')) closeCapturePanel()
    })

    // 拉文章列表填充下拉
    chrome.runtime.sendMessage({ type: 'wb-articles' }, function (resp) {
      if (resp && resp.ok && Array.isArray(resp.articles)) {
        resp.articles.forEach(function (a) {
          var opt = document.createElement('option')
          opt.value = a.id; opt.textContent = a.title
          $('wb-article').appendChild(opt)
        })
      }
    })

    $('wb-save').addEventListener('click', function () {
      var content = $('wb-content').value.trim()
      var translate = $('wb-translate').value.trim()
      var articleId = $('wb-article').value
      if (!/\([^)]+\)/.test(content)) {
        $('wb-err').textContent = '请至少用 (词-中文) 标注一个生词'
        return
      }
      $('wb-save').disabled = true
      $('wb-save').textContent = '存入中…'
      $('wb-err').textContent = ''
      chrome.runtime.sendMessage(
        { type: 'wb-save', payload: { content: content, translate: translate, articleId: articleId } },
        function (resp) {
          if (resp && resp.ok) {
            closeCapturePanel()
          } else {
            $('wb-save').disabled = false
            $('wb-save').textContent = '存入'
            $('wb-err').textContent = '存入失败：' + ((resp && resp.error) || '未知错误')
          }
        }
      )
    })
  }

  document.addEventListener('selectionchange', function () {
    clearTimeout(window.__wbSelTimer)
    window.__wbSelTimer = setTimeout(onSelectionChange, 250)
  })
  // 选区按钮点击外部时移除
  document.addEventListener('mousedown', function (e) {
    if (captureBtn && e.target !== captureBtn) removeCaptureBtn()
  }, true)
```

> 说明：捕获功能独立于"是否高亮启用"，始终可用（与 `enabled`/黑名单无关）。弹窗用 shadow DOM + `all:initial` 隔离宿主页样式。

- [ ] **Step 2: 重新加载扩展并人工验证**

1. Chrome 打开 `chrome://extensions` → 打开开发者模式 → 「加载已解压的扩展程序」选 `extension/` 目录（或对已加载的点「重新加载」）。
2. 打开任意英文网页，选中一句含你已学词的句子 → 出现「＋ 存入 WordBread」按钮。
3. 点按钮 → 弹窗出现，句子里你学过的词已自动变成 `(word-中文)`。
4. 可手动补标新词、填翻译、选文章（默认"网页收藏"）。
5. 点「存入」→ 成功后弹窗关闭。
6. 回到网页端「句子库」→ 展开"网页收藏"（或所选文章）→ 看到新句子；点「练习」可挖空练习。

- [ ] **Step 3: 失败路径验证**

1. 选一句但把所有括号删光 → 点存入 → 提示"请至少用 (词-中文) 标注一个生词"，不发请求。
2. （可选）断网后存入 → 弹窗显示"存入失败：…"，按钮恢复可用。

- [ ] **Step 4: Commit**

```bash
git add extension/content.js
git commit -m "feat(ext): 选区存句弹窗（智能预标注）"
```

---

## Task 4: 端到端回归 + 词表联动确认

**Files:** 无

- [ ] **Step 1: 闭环确认**

存入一句含**新词**（带新 `(word-中文)`）→ background 存完会自动 `refreshWordMap()`；几秒后在其它网页刷新，新词应被高亮（验证"收藏→词库→高亮"闭环）。

- [ ] **Step 2: 不污染宿主页确认**

在排版复杂的页面（如新闻站）打开弹窗，确认弹窗样式不被页面 CSS 影响、关闭后无残留节点（`document.querySelector` 检查 `div` 宿主已移除）。

---

## 自检记录

- **Spec 覆盖**：选区浮动按钮（Task 3）、智能预标注（Task 1 `buildAnnotatedContent` + Task 3 预填）、选目标文章默认"网页收藏"（Task 2 `ensureInboxArticle` + Task 3 下拉）、直接写 Supabase（Task 2，经 background 规避 CORS）、存进来即可练（复用网页端挖空逻辑）。
- **占位符**：无；REST 调用、消息协议、弹窗 DOM 均给出完整代码。
- **类型/协议一致性**：消息类型 `wb-articles`/`wb-save` 在 background 与 content 两侧一致；`wb-save` payload `{content, translate, articleId}` 字段一致；`wordMap[norm]={display,hint}` 与 background `refreshWordMap` 写入结构一致。
- **YAGNI 取舍**：v1 用可编辑预标注文本框微调；chip 式点选增删、多词短语可视化点选列为后续增强（不影响核心闭环）。
- **安全**：沿用既有公开 anon key 与 host 权限，无新增机密；写入字段与网页端 `createSentence` 对齐。
